version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:10.16.0
    working_directory: ~/homepage/frontEnd
    steps:
      - checkout
      - run: echo "hello world"
      - run: yarn build

